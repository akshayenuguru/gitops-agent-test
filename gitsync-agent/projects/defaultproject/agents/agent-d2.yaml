apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: agent-d2
  project: defaultproject
spec:
  active: true
  config:
    concurrency: 66
    docker:
      limits:
        cpu: "600"
        memory: 1Gi
    environment:
      numWorkers: 16
  type: Docker
  version: r3.7.0
