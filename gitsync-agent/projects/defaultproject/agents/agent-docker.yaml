apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: agent-docker
  project: defaultproject
spec:
  config:
    concurrency: 50
    docker:
      limits:
        cpu: "250"
        memory: 1Gi
    environment:
      numWorkers: 10
  type: Docker
  version: r4.0.0
