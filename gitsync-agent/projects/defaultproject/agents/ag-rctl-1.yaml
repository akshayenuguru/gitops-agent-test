apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: ag-rctl-1
  project: defaultproject
spec:
  cluster:
    name: c1-mks
  config:
    concurrency: 10
    environment:
      numWorkers: 10
    kubernetes:
      limits:
        cpu: "10"
        memory: 1Gi
  type: Cluster
  version: r4.0.1
