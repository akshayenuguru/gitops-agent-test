apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: a-k2
  project: defaultproject
spec:
  active: true
  cluster:
    name: c1-mks
  config:
    concurrency: 65
    environment:
      numWorkers: 15
    kubernetes:
      limits:
        cpu: "650"
        memory: 1Gi
  type: Cluster
  version: r4.0.1
