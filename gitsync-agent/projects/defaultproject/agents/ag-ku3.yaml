apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: ag-ku3
  project: defaultproject
spec:
  active: true
  cluster:
    name: c1-mks
  config:
    concurrency: 1
    environment:
      numWorkers: 1
    kubernetes:
      limits:
        cpu: "1"
        memory: 1Gi
  type: Cluster
  version: r3.7.0
