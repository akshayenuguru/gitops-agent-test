apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: a-k3
  project: defaultproject
spec:
  cluster:
    name: c1-mks
  config:
    concurrency: 30
    environment:
      numWorkers: 5
  type: Cluster
  version: r4.0.1
