apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: a-k3
  project: defaultproject
spec:
  cluster:
    name: c1-mks
  config:
    concurrency: 60
    environment:
      numWorkers: 60
  type: Cluster
  version: r3.7.0
