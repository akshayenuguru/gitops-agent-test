apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: doc-1
  project: akshay
spec:
  config:
    concurrency: 55
    docker:
      limits:
        cpu: 255m
        memory: 2Gi
    environment:
      numWorkers: 15
  type: Docker
  version: r4.0.0
